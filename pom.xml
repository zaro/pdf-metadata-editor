<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>pdf-metadata-edit</groupId>
	<artifactId>pdf-metadata-edit</artifactId>
	<version>2.0.0-beta</version>
	<name>Pdf Metadata Editor</name>
	<description>Simple PDF metadata editor</description>
	<url>http://broken-by.me/pdf-metadata-editor/</url>
	<packaging>jar</packaging>

	<developers>
		<developer>
			<id>zaro</id>
			<name>Svetlozar Argirov</name>
			<email>zarrro@gmail.com</email>
			<url>http://broken-by.me/</url>
			<properties>
				<picUrl>http://www.gravatar.com/avatar/75fa519466b509cfea3ef6cfbcbd066b</picUrl>
			</properties>
		</developer>
	</developers>
	<properties>
		<staging.dir>${project.build.directory}</staging.dir>
	</properties>

	<build>
		<sourceDirectory>src</sourceDirectory>
		<resources>
			<resource>
				<directory>src</directory>
				<excludes>
					<exclude>**/*.java</exclude>
					<exclude>**/*.properties</exclude>					
				</excludes>
			</resource>
			<resource>
				<directory>src/pmedit</directory>
				<includes>
					<include>*.properties</include>
				</includes>
				<filtering>true</filtering>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.3</version>
				<configuration>
					<source>1.6</source>
					<target>1.6</target>
				</configuration>
			</plugin>
			
			<!--  
				appbundler should run before shade, plugin so that it 
				includes the original and not shaded jar
			-->
			<plugin>
				<groupId>sh.tak.appbundler</groupId>
				<artifactId>appbundle-maven-plugin</artifactId>
				<version>1.1.0</version>
				<configuration>
					<mainClass>pmedit.Main</mainClass>
					<iconFile>pmedit/pdf-metadata-edit.icns</iconFile>
					<bundleName>PdfMetadataEditor</bundleName>
					<generateDiskImageFile>true</generateDiskImageFile>
					<includeApplicationsSymlink>true</includeApplicationsSymlink>
					<buildDirectory>${project.build.directory}/macos</buildDirectory>
					<diskImageFile>${project.build.directory}/dist/${project.build.finalName}.dmg</diskImageFile>
				</configuration>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>bundle</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>2.4</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<createDependencyReducedPom>false</createDependencyReducedPom>
							<minimizeJar>true</minimizeJar>
							<transformers>
								<transformer
									implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<manifestEntries>
										<Main-Class>pmedit.Main</Main-Class>
									</manifestEntries>
								</transformer>
							</transformers>
							<artifactSet>
								<excludes>
									<exclude>junit:junit</exclude>
									<exclude>org.apache.pdfbox:fontbox</exclude>
								</excludes>
							</artifactSet>
							<filters>
								<filter>
									<artifact>commons-logging:commons-logging</artifact>
									<includes>
										<include>org/apache/commons/logging/**</include>
										>
									</includes>
								</filter>
								<filter>
									<artifact>org.apache.pdfbox:pdfbox</artifact>
									<includes>
										<include>**</include>
										<include>org/apache/pdfbox/encoding/**</include>
									</includes>
									<excludes>
										<exclude>org/apache/pdfbox/pdfviewer</exclude>
									</excludes>
								</filter>
								<filter>
									<artifact>*:*</artifact>
									<excludes>
										<exclude>META-INF/*.SF</exclude>
										<exclude>META-INF/*.DSA</exclude>
										<exclude>META-INF/*.RSA</exclude>
									</excludes>
								</filter>
							</filters>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.codehaus.izpack</groupId>
				<artifactId>izpack-maven-plugin</artifactId>
				<version>5.0.9</version>
				<extensions>true</extensions>
				<configuration>
					<installFile>izpack/izpack-install.xml</installFile>
					<baseDir>${staging.dir}</baseDir>
					<mkdirs>true</mkdirs>
					<outputDirectory>${project.build.directory}/dist</outputDirectory>
					<finalName>pdf-metadata-edit-${project.version}-installer</finalName>
					<autoIncludeUrl>true</autoIncludeUrl>
					<autoIncludeDevelopers>true</autoIncludeDevelopers>
					<comprLevel>9</comprLevel>
				</configuration>
				<executions>
					<execution>
						<id>standard-installer</id>
						<phase>package</phase>
						<goals>
							<goal>izpack</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.7</version>
				<executions>
					<execution>
						<id>copy-resources</id>
						<phase>validate</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<encoding>UTF-8</encoding>
							<outputDirectory>${staging.dir}</outputDirectory>
							<resources>
								<resource>
									<directory>izpack/</directory>
									<excludes>
										<exclude>izpack-install.xml</exclude>
									</excludes>
								</resource>
								<resource>
									<directory>izpack/</directory>
									<includes>
										<include>*.ini</include>
										<include>*.txt</include>
										<include>*.hta</include>
										<include>pmedit-cli</include>
										<include>pdf-metadata-edit</include>
									</includes>
									<filtering>true</filtering>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<descriptor>izpack/assembly-zip.xml</descriptor>
					<finalName>dist/pdf-metadata-edit-${project.version}</finalName>
				</configuration>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			
			<plugin>
  				<artifactId>maven-antrun-plugin</artifactId>
  				   <executions>
					     <execution>
					      <id>create-exe</id>
					      <phase>install</phase>
					      <goals>
					       <goal>run</goal>
					      </goals>
					      <configuration>
					       <tasks>
					        <exec executable="7z">
					         <arg value="a" />
					         <arg value="-m0=lzma" />
					         <arg value="-mx=9" />
					         <arg value="-ms=on" />
					         <arg value="${project.build.directory}/installer.7z" />
					         <arg value="${project.build.directory}/dist/pdf-metadata-edit-${project.version}-installer.jar" />
					         <arg value="${project.build.directory}/installer.hta" />
					        </exec>
					        <concat
					         	destfile="${project.build.directory}/dist/pdf-metadata-edit-${project.version}-installer.exe"
				         		binary="yes">
						         <path>
						          <filelist dir="${basedir}" files="${project.build.directory}/7zsd-pmedit.exe,${project.build.directory}/config.txt,${project.build.directory}/installer.7z" />
						         </path>
					        </concat>
					       </tasks>
					      </configuration>
					     </execution>
					    </executions>
			</plugin>

			
		</plugins>


	</build>
	<dependencies>
		<dependency>
			<groupId>org.apache.pdfbox</groupId>
			<artifactId>pdfbox</artifactId>
			<version>2.0.2</version>
		</dependency>
		<dependency>
			<groupId>org.apache.pdfbox</groupId>
			<artifactId>xmpbox</artifactId>
			<version>2.0.2</version>
		</dependency>
		<dependency>
			<groupId>com.miglayout</groupId>
			<artifactId>miglayout-swing</artifactId>
			<version>4.2</version>
		</dependency>
		<dependency>
			<groupId>com.toedter</groupId>
			<artifactId>jcalendar</artifactId>
			<version>1.4</version>
		</dependency>
		<dependency>
			<groupId>org.yaml</groupId>
			<artifactId>snakeyaml</artifactId>
			<version>1.14</version>
		</dependency>
		<dependency>
			<groupId>com.googlecode.json-simple</groupId>
			<artifactId>json-simple</artifactId>
			<version>1.1.1</version>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.12</version>
		</dependency>
		<dependency>
			<groupId>net.java.dev.jna</groupId>
			<artifactId>jna-platform</artifactId>
			<version>4.2.1</version>
		</dependency>
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpclient</artifactId>
			<version>4.5</version>
		</dependency>
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpasyncclient</artifactId>
			<version>4.1</version>
		</dependency>

	</dependencies>
</project>